{
    "name": "magnitude",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "deploy": "vite build && gh-pages --before-add ./before-add.cjs -d dist"
    },
    "devDependencies": {
        "gh-pages": "^6.1.1",
        "typescript": "^5.5.3",
        "vite": "^5.4.0"
    },
    "dependencies": {
        "bitecs": "^0.3.40",
        "flatqueue": "^2.0.3"
    }
}
